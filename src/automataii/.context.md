# Context: src/automataii/

This directory, `automataii/`, is the root of the `automataii` Python package. It contains all the core source code, modules, and sub-packages that constitute the `automataii` library and application.

## Purpose:

- **Primary Codebase**: Houses the main logic, algorithms, and functionalities of the project.
- **Python Package**: Structured to be installable and importable as a Python package (e.g., `import automataii`).

## Key Files:

- **`__init__.py`**: Marks this directory as a Python package. It might also define the public API of the `automataii` package by importing key classes or functions from its submodules, or it could be empty.
- **`__main__.py`**: If present and executable, this file provides a command-line entry point for the `automataii` package. This allows the package to be run directly using `python -m automataii`.
- **`print_sys_path.py`**: A utility script, likely for debugging Python's module search path (`sys.path`).

## Core Subdirectories (Modules/Sub-packages):

Based on the provided `tree` output, this `automataii` package is organized into several specialized modules:

- **`animate/`**: Handles animation creation, processing, and management.
- **`carsegnet/`**: Contains components related to car segmentation, likely involving a machine learning model.
- **`config/`**: Manages application configuration, settings loading, and parameter definitions (currently contains `z_indices.py`).
- **`core/`**: Contains the fundamental business logic, core data structures (models), and essential algorithms.
- **`generation/`**: Modules related to procedural generation of components like linkages, gears, cams.
- **`gui/`**: All components related to the Graphical User Interface.
- **`kinematics/`**: Focuses on kinematics, including inverse kinematics (IK) and solvers.
- **`models/`**: Stores data models, machine learning model definitions and weights (e.g., `.pth` files, configuration for detectors and pose estimation).
- **`utils/`**: A collection of utility functions and helper classes.

## Development Guidelines for this Package:

- **Modularity**: Each subdirectory should have a clear, well-defined responsibility.
- **API Design**: The public API exposed by `automataii/__init__.py` should be carefully considered.
- **Relative Imports**: Use relative imports for intra-package dependencies (e.g., `from .core import ...`, `from ..utils import ...`).
- **Testing**: Unit tests and integration tests for the modules within `automataii` should ideally be located in a separate `tests/` directory at the project root (not visible in the `src` tree provided, but good practice).
- **Documentation**: Docstrings should be used for all modules, classes, and functions. `.context.md` files like this one serve as directory-level documentation.
- **Adherence to Project Rules**: All code within this package must adhere to the overall project guidelines, coding standards, and role-specific instructions.